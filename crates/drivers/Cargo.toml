[package]
name = "drivers"
version = "0.1.0"
edition = "2024"

[lib]
test = false
doctest = false
bench = false

[dependencies]
sync = { path = "../sync" }
fs = { path = "../fs" }
types = { path = "../types" }
arch = { path = "../arch" }
param = { path = "../param" }
