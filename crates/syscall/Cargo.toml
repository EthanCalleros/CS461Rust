[package]
name = "syscall"
version = "0.1.0"
edition = "2024"

[lib]
test = false
doctest = false
bench = false

[dependencies]
proc  = { path = "../proc" }
param = { path = "../param" }
types = { path = "../types" }
arch  = { path = "../arch" }