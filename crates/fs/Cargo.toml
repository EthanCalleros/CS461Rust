[package]
name = "fs"
version = "0.1.0"
edition = "2024"

[lib]
test = false
doctest = false
bench = false

[dependencies]
types = { path = "../types" }
param = { path = "../param" }
sync = { path = "../sync" }
